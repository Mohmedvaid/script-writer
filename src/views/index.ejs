<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Bedtime‑History Generator</title>
  <script defer src="/main.js"></script>
  <style>
    body {
      font-family: system-ui, sans-serif;
      max-width: 48rem;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    h1 {
      text-align: center;
    }

    label {
      display: block;
      margin-top: 1rem;
      font-weight: 600;
    }

    input {
      width: 100%;
      padding: .5rem;
      font-size: 1rem;
    }

    button {
      margin-top: 1.5rem;
      padding: .7rem 1.4rem;
      font-size: 1rem;
    }

    pre {
      white-space: pre-wrap;
      background: #f5f5f5;
      padding: 1rem;
      border-radius: .5rem;
    }
  </style>
</head>

<body>
  <h1>Generate Sleep‑Friendly Video</h1>

  <form id="genForm">
    <label>Video title
      <input name="title" required placeholder="e.g. Mid-Winter Troll Taxes" />
    </label>

    <label>Mood
      <input name="mood" placeholder="leave blank to auto-pick" />
    </label>

    <label>POV
      <select name="pov">
        <option value="second_person" selected>Second-person</option>
        <option value="first_person">First-person</option>
      </select>
    </label>

    <label>Image style
      <select name="styleKey">
        <option value="tapestry" selected>Tapestry</option>
        <option value="manuscript">Manuscript</option>
        <option value="etching">Etching</option>
      </select>
    </label>

    <button type="submit">Generate</button>
  </form>


  <pre id="log"></pre>
</body>

</html>